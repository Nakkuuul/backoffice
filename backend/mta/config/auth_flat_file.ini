; Credentials the backoffice app uses to authenticate before relaying.
; These MUST match SMTP_USER / SMTP_PASS in the app's .env.
; CHANGE the password before any real use.
[core]
methods=PLAIN,LOGIN

[users]
relay@yourdomain.com=CHANGE_ME_STRONG_PASSWORD
