# Local (inbound) domains Haraka accepts mail FOR. Mail to these is forwarded
# to the app webhook (bounces/complaints/replies). Mail to any other domain is
# only accepted from trusted/relaying clients (our app) for outbound delivery.
sapphirebroking.net
