[main]
; The backoffice app's inbound webhook. host.docker.internal = the on-prem host
; running the app (Docker Desktop). On Linux compose, use the host gateway or
; the app's address. Must match EMAIL_INBOUND_SECRET in the app's .env.
url=http://host.docker.internal:3000/api/v1/email/inbound
token=b1c9a12635cf286c9fe11d92158f2d23d8e413e7f881fd69
