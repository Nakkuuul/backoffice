; STARTTLS for the submission listener and for outbound delivery.
; Provide a real cert/key on deploy (paths are relative to the config dir).
; For outbound, opportunistic TLS to remote MX is used automatically.
;
; key=tls_key.pem
; cert=tls_cert.pem
