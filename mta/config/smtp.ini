; Submission listener for the backoffice app. Bound to localhost via compose.
listen=0.0.0.0:2525

; Idle socket timeout (seconds).
inactivity_timeout=300

; Single process handles on-prem submission fine; for higher throughput set
; nodes=cpus (one worker per core).
;nodes=cpus
